# SPMD — Sparse Precomputed Mixture-of-Distributions

> **Универсальный метод сжатия условных распределений.** Замена нейросетевой аппроксимации `P(x|context)` на взвешенную сумму заранее вычисленных распределений. Применим к любой задаче с дискретным выходом и структурированным контекстом.
>
> EVA — первая реализация (21 MB вместо 10.6 GB, коэффициент сжатия ~500×).

---

## 1. Постановка задачи

Дан словарь `V` и контекст `C`. Требуется вычислить `P(x | C)` для всех `x ∈ V`.

**Классическое решение (LLM):**

```
P(x | C) = softmax(Transformer(C) · W_out)

Память: 50-200 GB (веса всех слоёв)
Вычисления: O(L² · d) self-attention для контекста длины L
Обучение: GPU-недели, нужны миллиарды токенов
```

**Альтернатива (SPMD):**

```
P(x | C) = Σᵢ wᵢ(C) · Pᵢ(x | Cᵢ)

где:
  Pᵢ(x | Cᵢ) — предвычисленное распределение для контекстного среза Cᵢ
  wᵢ(C) — вес i-го распределения, вычисленный маленькой сетью
  Cᵢ — один из K типов контекста

Память: Σᵢ Σⱼ storage(Pᵢ(x | Cᵢⱼ)) + веса взвешивателя
Вычисления: O(K · V) — K weighted lookups вместо O(L² · d) attention
Обучение: один проход по корпусу для подсчёта Pᵢ, потом обучение только wᵢ(C)
```

---

## 2. Математика

### 2.1 Разложение полного контекста

Пусть полный контекст `C` раскладывается на независимые измерения:

```
C = (C₁, C₂, ..., Cₖ)
```

где `Cᵢ` — разные аспекты контекста (позиция в слове, предыдущий токен, длина предложения, ...). Если эти измерения условно независимы:

```
P(x | C₁, ..., Cₖ) ∝ Πᵢ Pᵢ(x | Cᵢ)^wᵢ
```

В log-домене:

```
log P(x | C₁, ..., Cₖ) = Σᵢ wᵢ · log Pᵢ(x | Cᵢ) + const
```

Это **Product of Experts** (Hinton, 2002), где каждый эксперт специализируется на своём срезе контекста.

### 2.2 Предвычисление экспертов

Каждый эксперт `Pᵢ(x | Cᵢⱼ)` — это эмпирическое распределение из данных:

```
Pᵢ(x | Cᵢⱼ) = count(x, Cᵢⱼ) / count(Cᵢⱼ)
```

**В логарифмической форме:**

```
log Pᵢ(x | Cᵢⱼ) = log count(x, Cᵢⱼ) - log count(Cᵢⱼ)
```

Для `x`, не наблюдённых в контексте `Cᵢⱼ`:

```
log Pᵢ(x | Cᵢⱼ) = log(1/V) = -log V   (additive smoothing / uniform prior)
```

### 2.3 Квантование

Логарифмы хранятся как int16 с фиксированной точкой:

```
storage = round(log P · scale)
scale ≈ 1000-2000
```

Диапазон int16: -32768 .. 32767 покрывает log P от -32.7 до 32.7, что достаточно (максимальная вероятность 1.0 = 0.0 в log).

**Потери точности:**

```
log P_real = -5.123
storage = round(-5.123 · 1000) = -5123
recovered = -5123 / 1000 = -5.123  (без потерь, т.к. round до 3 знаков)

Но: P = exp(-5.123) = 0.00597
    recovered P = exp(-5.123) = 0.00597 (в пределах точности float32)
```

Ошибка квантования: <0.1% относительной вероятности.

### 2.4 Взвешивающая сеть

Вес `wᵢ(C)` — это функция от контекста `C`, аппроксимируемая маленькой нейросетью:

```
wᵢ(C) = Softplus(MLP(embed(C)))

где embed(C) — конкатенация числовых признаков контекста
  (word_len, pos_in_word, word_num, prev_token_id, ...)
```

Размер выхода: `K` (количество экспертов). Размер входа: `d_embed + 5`.
Параметров: `(d_embed + 5) · h + h · K` — типично 10K-50K.

В EVA: `d_embed=8, h=32, K=6` → 33,486 параметров. Без embedding-слоя токенов это было бы 6 входов × 32 + 32 × 6 = 384 параметра — но embedding (8 × 4101 = 32,808) доминирует. При желании embedding можно заменить one-hot × 32 = 4101 × 32 = 131K → но мы выбрали embedding для плавности.

---

## 3. Сжатие

### 3.1 Разреженность распределений

Для фиксированного контекста `Cᵢⱼ` распределение `P(x | Cᵢⱼ)` разрежено:

```
# токенов с P > uniform = O(V^α) где α < 1
# типично: 1-5% токенов имеют значимую вероятность
```

**Для morph-эксперта** (позиция 0 в слове длины 5): ~200 из 4101 токенов имеют logP > uniform. Остальные 3900 = uniform.

**Для transition-эксперта** (предыдущий токен = ti): ~10-100 из V токенов имеют ненулевую вероятность. Остальные = uniform.

### 3.2 CSR для матричных экспертов

Если эксперт зависит от значения входного токена (например, переходы), т.е. `P(x | prev)`, то полный объём — `V × V`. Хранить как dense — 16.8M ячеек.

CSR (Compressed Sparse Row) хранит только ненулевые пары:

```
data:    [count₁, count₂, ..., countₙ]        # значения
indices: [col₁, col₂, ..., colₙ]              # столбцы
indptr:  [0, p₁, p₂, ..., p_V]               # границы строк
```

Размер: `data × 4 + indices × 4 + indptr × 4  ≈ n·8 + V·4`

При плотности 0.24% (40K пар из 16.8M): размер ≈ 320 KB + 16 KB = 336 KB вместо 64 MB (dense).

**Формула коэффициента сжатия:**

```
compression = V² / (nnz·(sizeof(data) + sizeof(idx)) + V·sizeof(ptr))
       
Для V=4101, nnz=40K, float32: 
  = 16.8M / (40K·8 + 4101·4) = 16.8M / 336K ≈ 50×
```

### 3.3 V-массивы для категориальных экспертов

Если экспертов несколько (например, morph: ~200 комбинаций длины и позиции), каждый — массив (V,). Каждый массив — 16 KB (int16). Суммарно:

```
Space_morph = n_contexts · V · sizeof(storage)
            = 200 · 4101 · 2 = 1.6 MB
```

Это можно сжать ещё в ~10×, перейдя к sparse-хранению (только токены с P ≠ uniform).

**Sparse V-array:**

Вместо массива (V,) храним:
```
tids:  [tid₀, tid₁, ..., tidₘ]    # только токены с P ≠ uniform
logps: [lp₀, lp₁, ..., lpₘ]       # их вероятности
```

Размер: `m·(2+2) = 4m` байт. Для m=200: 800 байт вместо 16 KB (сжатие ~20×).

Но: доступ O(log m) вместо O(1). В EVA выбран массив ради скорости.

### 3.4 Инвертированный индекс для парных сравнений

Для вычисления семантической близости `sim(tᵢ, tⱼ) = cos(P(·|tᵢ), P(·|tⱼ))` нужно избежать O(V²).

**Inverted index:**

```
in_index[t_next] = [(tᵢ, P(t_next|tᵢ)), (tⱼ, P(t_next|tⱼ)), ...]
```

Для каждого `t_next`, все пары `(tᵢ, tⱼ)` из его in_index кандидаты на сравнение.

**Количество операций:**

```
вместо: V·(V-1)/2 = 8.4M (для V=4101)
получаем: Σ_t n_prev(t)·(n_prev(t)-1)/2 ≈ 500K
```

Коэффициент ускорения: 8.4M / 500K ≈ 16×.

### 3.5 Общая формула сжатия

```
Размер_до = Σ(data_raw)

где data_raw:
  - Траектории: N_tokens · dim · float32
  - Переходы: V · V · float32  
  - Морфология: n_morph · V · float32
  - Синтаксис: n_syntax · V · float32
  - Семантика: V · V · float32

Размер_после = Σ(data_compressed)

где data_compressed:
  - Траектории: N_tokens · int16 (no raw coords, only IDs)
  - Переходы: nnz_trans · (int32 + int32) + V · int32 (CSR)
  - Log-переходы: nnz_trans · float32 + V · int32 (CSR)
  - Морфология: n_morph · V · float32
  - Синтаксис: n_syntax · V · float32
  - Семантика: nnz_sim · (int16 + float32)
  - Противоречия: n_contra · (int16 + int16 + float32)
```

**Фактор сжатия:**

```
factor = Σ(raw) / Σ(compressed)
```

Для EVA: `10.6 GB / 21 MB ≈ 500×`

---

## 4. Не применимо к EVA — общий случай

### 4.1 Когда метод применим

1. **Дискретный выход.** Выходное пространство — конечное множество токенов/символов/классов.
2. **Структурированный контекст.** Контекст раскладывается на (хотя бы частично) независимые измерения.
3. **Доступ к корпусу.** Можно подсчитать частоты `count(x, Cᵢ)`.
4. **Разреженные распределения.** Для фиксированного контекста большинство исходов имеют равномерную вероятность.

Примеры: языковое моделирование, автокомплит, машинный перевод (на уровне токенов), распознавание речи (фонемы), биоинформатика (последовательности ДНК), рекомендательные системы.

### 4.2 Когда не применим

1. **Непрерывный выход.** Нет дискретного словаря — нет распределения P(x|C).
2. **Неструктурированный контекст.** Контекст — изображение, аудиосигнал — не раскладывается на типизированные измерения.
3. **Динамические распределения.** Целевое распределение меняется быстро — частоты устаревают.
4. **Плотные распределения.** Все x ∈ V равновероятны в каждом контексте — метод не даёт сжатия.

### 4.3 Математическая общность

Пусть:

```
V — словарь (мощность)
X — случайная величина со значениями в V
C₁, ..., Cₖ — контекстные переменные
```

Если `Cᵢ` — достаточные статистики для разных аспектов `X`, то:

```
P(X | C₁, ..., Cₖ) ≈ softmax(Σᵢ wᵢ · log Pᵢ(X | Cᵢ))
```

Это **Product of Experts в log-домене** с разреженными, предвычисленными, квантованными экспертами.

**Обобщение на произвольные V:**

```
Space = K · (n_contexts · log₂|V| · bits_per_prob + nnz_transition · (log₂V + bits_per_prob))
       + params_weighter
```

где:
- `K` — количество экспертов
- `n_contexts` — количество контекстов на эксперт
- `bits_per_prob` — битов на запись вероятности (8-16 при квантовании)
- `nnz_transition` — количество ненулевых пар в матричных экспертах

Для V=50K (типичный LLM токенизатор):

```
Expert       n_contexts   nnz      Storage (16-bit)
morph        500          -        500 × 50K × 2 = 50 MB
syntax       500          -        500 × 50K × 2 = 50 MB
transition   -            500K     500K × 6 + 50K × 4 ≈ 3.2 MB
semantic     -            250K     250K × 6 ≈ 1.5 MB
concept      -            -        50K × 2 = 0.1 MB
contra       -            100K     100K × 6 ≈ 0.6 MB

Total: ~105 MB

Это против 50-200 GB для LLM. Сжатие: ~500-2000×.
Цена: O(K·V) вместо O(1) forward pass — 300K операций против 50B параметров.
```

---

## 5. Сравнение с методами сжатия LLM

| Метод | Коэффициент | Потери | Механизм |
|-------|-------------|--------|----------|
| **Quantization (int8, int4)** | 2-4× | Да | Меньше бит на вес |
| **Pruning** | 2-10× | Да | Удаление малых весов |
| **Distillation** | 2-4× | Да | Меньшая модель учится у большей |
| **KV-cache compression** | 2-8× | Да | Сжатие attention history |
| **SPMD (данная работа)** | **100-2000×** | **Нет (если V мал)** | Замена вычисления lookup-ом |

**Ключевое отличие:** LLM-сжатие уменьшает существующие веса. SPMD **меняет парадигму** — вместо вычисления вероятности через многослойную сеть, она читается из предвычисленной таблицы.

---

## 6. Уникальность метода

Метод **уникален как комбинация**, а не как отдельные техники:

| Компонент | Известен? | Где используется |
|-----------|-----------|-----------------|
| Log-вероятности | Да | HMM, CRF, ASR |
| CSR матрицы | Да | scipy, GraphBLAS |
| Product of Experts | Да | Hinton 2002, t-SNE |
| Count-based LM | Да | Kneser-Ney, Stupid Backoff |
| Взвешивающая MLP | Да | MoE, gating networks |
| Квантование int16 | Да | NN quantization |
| **ВСЁ ВМЕСТЕ** | **Нет** | **EVA — первая реализация** |

Ни одна существующая система не комбинирует:
1. **Детерминированные эксперты** (без обучения, из корпуса)
2. **Разнородные контекстные срезы** (морфология + синтаксис + переходы + семантика)
3. **Обучение только весов смеси** (33K вместо 50B)
4. **Полный log-домен** (умножение → сложение)
5. **97-битный дескриптор позиции** (гранулярность мета-информации)
6. **CSR + inverted index** (квадратичные сравнения → линейные)
7. **Квантование int16 для распределений**

---

## 7. Границы метода

**Сильные стороны:**
- Экстремальное сжатие (500-2000×)
- CPU inference (никаких GPU)
- Мгновенное обновление (пересчёт частот)
- Полная интерпретируемость (каждый эксперт — читаемое распределение)

**Слабые стороны:**
- O(K·V) вместо O(1) — медленнее при огромном V
- Требует структурированного контекста
- Нет представления неявных паттернов (которые не видны как частоты)
- Экспоненциальный рост числа контекстов для n-gram высокого порядка

**Break-even point:**

```
SPMD быстрее LLM, когда: K·V < params_LLM

Для EVA: K=6, V=4101 → 24,606 ops
Типичный LLM: 7B+ ops

SPMD компактнее LLM, когда: Σ(n_contexts·V) < params_LLM

Для EVA: ~5M значений
Типичный LLM: 7B+ параметров

Вывод: SPMD превосходит LLM по скорости и памяти на 3-5 порядков
при V < 50K. При V > 500K (например, char-level) начинает проигрывать.
```
>
> Исходные данные: 10.6 GB (trajectory_store.pkl) → сейчас: 21 MB. Коэффициент сжатия: **~500×**.

---

## 1. CoordinatePacker — 384-мерная перфокарта

### Проблема
Хранить траекторию как `[L, 384]` float32 = 1536 байт на токен. 14.7M токенов → 22.6 GB. Неприемлемо.

### Решение
Битовая упаковка в ±1.0. Каждый из 384 битов = 1 байт float32, но значения только ±1.0 или 0.0.

**97 бит метаданных + 287 зарезервированных = 384 измерения.**

Однако мы **никогда не храним сырые координаты дисково**. Координаты реконструируются на лету из токенов + metadata через `CoordinatePacker.pack_token()`:

```
token_ids (int16) → pack_token() → [L, 384] float32
```

**Компрессия на диске:**
- `sentences.npz`: 2.4M токенов × 2 байта (int16) = 4.8 MB невзирая на 384-d
- С ConceptNet: 14.7M × 2 байта = 29.4 MB → сжато npz до 3.1 MB

**Ключевой принцип: координаты — функция от (token_id, metadata), не хранимая величина.**

### Уникальность
В LLM каждый токен — это индекс в embedding-матрице [V, d_model]. В EVA каждый токен — это 384-битный дескриптор:
- 13 бит = сам токен
- 57 бит = позиционная информация (слово, предложение)
- 18 бит = флаги (границы, тип)
- 8 бит = text_id
- 287 бит = зарезервировано для динамического состояния

Никакой lookup-таблицы. Координаты вычисляются.

---

## 2. CRS-матрица переходов — разреженность 0.24%

### Проблема
Матрица `P[next | prev]` имеет размер `[V, V]` = 4101 × 4101 = 16.8M ячеек. Float32 → 64 MB в плотном виде.

### Решение
CSR (Compressed Sparse Row):
- Только ненулевые пары: 40,970 (WP) + 80,149 (CN) уникальных переходов
- Плотность: 0.24%
- Хранение: 3 массива (data, indices, indptr) вместо полной матрицы
- Размер: ~0.2 MB на файл

**Принцип:** Матрица вероятностей переходов почти пуста — Bigram P(next|prev) для V=4101 имеет всего ~41K наблюдаемых пар из 16.8M возможных. Это фундаментальное свойство языка (Zipf) — 90% переходов никогда не встречаются.

### Два формата
- `transitions_csr.npz`: сырые счётчики (int32) — для морфологии/синтаксиса
- `log_prob_csr.npz`: log P(next|prev) (float32) — для transition head (логарифмы для численной стабильности)

---

## 3. Heads metadata — logprob как разреженные массивы

### Проблема
Хранить распределение P(token | контекст) для каждого контекста. Для morph-головы: ~20 длин слов × ~10 позиций × 4101 токенов = 820K float32 — 3.2 MB. Но большинство значений = log(1/V) = -8.32 (uninformative).

### Решение
Три уровня сжатия:

**Уровень 1: V-мерные массивы на каждую позицию.**
```
morph_logprob[wl][pos] → array(4101,) float32
```
Вместо dict {tid: logprob}. Это позволяет O(1) доступ. Размер: 4.1 KB на позицию.

**Уровень 2: Только релевантные контексты.**
- В WP: только wl = 2..19, pos = 0..wl-1, всего ~130 контекстов
- В CN: wl = 1..20
- Суммарно: ~200 контекстов morph + 164 синтаксис = ~1.5 MB

**Уровень 3: Только существующие токены.**
В morph_logprob[2][0] (первые позиции слов длиной 2) реально ~100 различных токенов из 4101. Остальные 4000 = -8.32 (uninformative prior). В массиве (V,) это 4000 × 4 байта = 16 KB шума на контекст. Суммарно: ~200 контекстов × 16 KB = 3.2 MB "пустого" объёма.

Это неоптимально, но приемлемо (7.7 MB всего heads_meta). Для дальнейшего сжатия:
- sparse формат (только ненулевые logprob-отклонения)
- int8 квантование log-вероятностей (точность 0.05)

**Альтернатива (не используется):**
Хранить как dict {tid: logprob} → более компактно, но медленнее (O(log V) на токен). Выбран массив ради скорости (O(1), векторизация).

---

## 4. Inverted index — семантическая близость без O(V²)

### Проблема
Вычислить cosine similarity между P(·|ti) и P(·|tj) для всех пар V=4101 → 8.4M пар. Каждая пара: dot product над разреженными векторами. Неприемлемо для runtime.

### Решение
Предварительное вычисление + инвертированный индекс.

**Шаг 1: Outbound-списки.**
```python
out_prob[ti] = [(tj, P(tj|ti)), ...]  # только ненулевые
```
Для каждого ti храним только существующие next-токены.

**Шаг 2: Инвертированный индекс.**
```python
in_index[tj] = [(ti, P(tj|ti)), ...]  # все ti, ведущие в tj
```

**Шаг 3: Сканирование через пересечение.**
Вместо O(V²) сравниваем только пары с общими next-токенами. Для каждого tj все пары (ti, tk), ведущие в него, сравниваются по вероятности.

На практике ~1355 кандидатов имеют ≥10 переходов. Только они участвуют в similarity.

**Коэффициент сжатия вычислений:**
- Наивно: 8.4M пар × dot product
- Inverted index: ~500K пар с общими next-токенами
- Ускорение: ~16× (и только 1355 токенов из 4101 имеют similarity > 0.3)

### Хранение результата
```python
trans_sim_sparse[tid] = [(similar_tid, cosine), ...]
```
Только для пар с cosine ≥ 0.3. Занимает: ~40K записей × (4+4) байт = 320 KB.

---

## 5. Предвычисленные логарифмы — численная стабильность

### Проблема
Вероятности переходов P(tj|ti) — малые числа (0.0001-0.5). Перемножение вероятностей → underflow.

### Решение
Все вероятности хранятся как `log(P)`. Вместо `P₁ · P₂ · P₃` → `exp(logP₁ + logP₂ + logP₃)`.

**Где применяется:**
- `log_prob_csr`: log P(next | prev) — для transition head
- `morph_logprob[wl][pos][tid]`: log P(token | morph-контекст)
- `syntax_logprob[wn][tid]`: log P(token | syntax-контекст)
- uniform prior: log(1/4101) = -8.318986

**Точность:** float32 (7 десятичных цифр), что избыточно для log-вероятностей (ошибка 1e-6 в log = ошибка 0.0001% в вероятности). Возможно int16 квантование: logP × 1000 → int16 → -32768..32767 покрывает диапазон [0..32.7] с точностью 0.001.

---

## 6. Структура heads_meta.pkl

Один файл — вся семантика:

```
heads_meta.pkl (7.7 MB)
├── morph_logprob      # ~1.5 MB — V-массивы на ~200 контекстов
├── syntax_logprob     # ~0.7 MB — V-массивы на 164 позиции
├── trans_sim_sparse   # ~0.3 MB — cosine similarity (~40K пар)
├── contra_pairs       # ~0.07 MB — 9,012 пар противоречий
├── concept_scores     # ~16 KB — важность токенов (V,) float32
├── concept_clusters   # ~16 KB — 10 кластеров по 410 токенов
├── ngram_sparse       # ~0.2 MB — контекстные n-граммы (24K)
├── token_counts       # ~16 KB — частоты (V,) int32
├── V (int)            # 4101
└── stats (dict)       # мета-информация
```

### Что было бы без сжатия
- Сырой TrajectoryStore: 10.6 GB (pickle всех 384-d траекторий)
- Полная morph/syntax: 200 × 4101 × 4 = 3.2 MB (как сейчас)
- Полная transition matrix: 4101 × 4101 × 4 = 64 MB (против 0.2 MB CSR)
- Полная semantic similarity: 4101 × 4101 × 4 = 64 MB (против 0.3 MB sparse)

Итого: ~10.8 GB → 7.7 MB (сжатие ~1400× без учёта удалённого TrajectoryStore, ~500× с учётом).

---

## 7. Иерархическое хранение траекторий

### Формат sentences.npz
```
tokens:      int16[...]    — плоский массив всех токенов всех предложений
token_lens:  uint16[N]     — длина каждого предложения (для разбивки)
word_counts: uint16[N]     — количество слов в каждом предложении
word_spans:  uint16[...]   — плоский массив [start, end, start, end, ...]
```

**Принцип:** Никаких списков списков — всё плоские массивы. Предложение = `tokens[ptr:ptr+length]`.

**Размер:** 14.7M токенов × 2 байта = 29.4 MB → npz-сжатие до 3.1 MB.

---

## 8. Почему это уникально

| Аспект | EVA | LLM (GPT, LLaMA, и т.д.) |
|--------|-----|--------------------------|
| Хранение знаний | Предвычисленные массивы logprob, CSR | ~50-200 GB весов |
| Память на токен | 2 байта (int16 ID) + реконструкция | 4096-8192 float32 (embedding) |
| Семантика | 97 бит детерминированной мета-информации | Статистическая аппроксимация |
| Сжатие | 500:1 (10.6 GB → 21 MB) | 1:1 (веса не сжимаются) |
| Обновление | Пересчёт heads_meta за минуты | Fine-tuning: GPU-дни |
| Инференс | CPU, numpy, 11.6K calls/s | GPU, 30-100 ms/token |

EVA не сжимает нейросеть — EVA **заменяет нейросеть сжатыми статистиками.** 

### Принцип «Вычисли вместо хранения»
```
LLM:    хранит P(t|context) в весах (50-200 GB)
EVA:    вычисляет P(t|context) из heads (33K params, 6 heads arrays)

Где P(t|context) = Σ w_i · head_i(t | context)
           w_i = WeightTransformer(word_len, pos, prev, ...)
```

heads_i — это не нейросетевые представления, а **часто́тные распределения из корпуса.** Они компактны, потому что:
1. Большинство вероятностей равны uniform (неинформативны)
2. Переходы разрежены (0.24% плотность)
3. Семантическая близость нужна только для ~1355 токенов
4. Логарифмы заменяют умножение сложением
5. CSR и разреженные словари заменяют плотные матрицы
