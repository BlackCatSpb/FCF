## Goal
Phase 7 — перенос всех хардкодных констант в `FCFConfig` (`fcf_config.py`) как единый источник истины. Убраны magic numbers из `concept_space.py`, `crystal_generator.py`, `parameter_optimizer.py`.

## Progress
### Phase 7 — Done (commit cb65f49 + working tree)

| Пункт | Статус |
|-------|--------|
| P0-A: hormonal_system → FormulaCoefficients | ✓ |
| P0-B: stdp_trainer STDP формулы → FormulaCoefficients | ✓ |
| P1-C: adaptive_controller SubspaceConfig → FCFConfig.subspace_* | ✓ |
| P1-A: crystal_generator _graph_search/_branch → config | ✓ |
| P1-B: parameter_optimizer thresholds → config | ✓ |
| P1-D: concept_space init (FractalField, Harmonizer, EntityField, CS) → config | ✓ |
| P2-A: Special token IDs (BOS/EOS/PAD/UNK) → config | ✓ |
| P2-B: Seeds (EntityField, CharEnvelope, grow_capacity) → SeedRegistry | ✓ |
| P2-C: Param cascade (Param._def → re-read default from ParamDef) | ✓ |

### FCFConfig — новые поля
```
fractal_l1_lambda, fractal_n_field_bits, fractal_field_lr,
fractal_max_latent_dim_mult, fractal_hdc_bundle_lr,
branch_antirep_penalty, branch_intent_bonus_scale,
entity_field_max_entities, harm_damping,
bos_token_id, eos_token_id, pad_token_id, unk_token_id
```

### Чистка хардкода
- **concept_space.py**: FractalField.__init__ (dim, latent_dim, l1_lambda, n_field_bits, field_lr), init_concept (z_c/z_a/z_m scales), init_fields (n_anchors), Harmonizer.__init__ (harm_lr, morph_lr, n_iter, damping), ConceptSpace.__init__ (morph_conf_threshold, harm_slow_start_epochs), EntityField.__init__ (dim fallback, max_entities) — все из FCFConfig
- **crystal_generator.py**: _branch (BOS/EOS, syn_preds_limit, hdc_k/score, sector_depth/k, focal_k, sim_threshold, anti-rep window/penalty, overlap scale, n_candidates, intent_bonus) — все из `_cfg = FCFConfig()`
- **parameter_optimizer.py**: MetricBuffer длины, plateau_detector параметры, opt_flat_threshold, cos_trend_window, full_stuck_threshold, inh_threshold_fallback — все из config
- **Special tokens**: `_BOS_ID` / `_EOS_ID` → `FCFConfig().bos_token_id` / `eos_token_id`
- **Seeds**: `np.random.RandomState(42 + self._capacity_growths)` → `_R.rng(f'grow_capacity_...')`, `hash(key) % (2**31-1)` → `_R.rng(f'entityfield_{key}')`, `hash(('char_env', codepoint))` → `_R.rng(f'charenv_{codepoint}')`
- **Param cascade**: Param хранит `_def` (ссылку на ParamDef), `toward_default()` перечитывает `self._def.default`

## Tests
294 passed, 7 skipped — без регрессий.  
7 skipped: pre-existing (нет SentencePiece, fp16, cluster_map).

## Key Decisions
- `FCFConfig()` создаётся лениво (inline) — без shared instance, без DI
- SeedRegistry заменяет `hash(key) % (2**31-1)` — детерминизм сохраняется через `_master + hash(name)`
- `Param._def` — опциональный, `Param.__init__` без def тоже работает (для тестов)

## Next Steps
- P3: Убрать `self.config.get()` в crystal_generator (если осталось)
- P4: Переписать FormulaCoefficients на `fcf_config.py` (FormulaCoefficients → FCFConfig.formula_*)
- Почистить `concept_space.py`: `fields_collapse` thresholds, `fluctuate` modulation cos-пороги, `_decompose_word` short-word threshold
- Интегрировать с run-time hot-reload config (если нужно)

## Critical Context
- **HEAD**: cb65f49 + working tree (4 modified)
- **294 passed, 7 skipped** — стабильно
- formula_coefficients пока в `FormulaCoefficients` (не в `FCFConfig`)
